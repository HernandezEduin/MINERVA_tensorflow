# MINERVA (NLQ) requirements for Python 3.9.x (conda env: minerva)
# Aligned with current imports under code/ and runtime parquet usage.

# Core numerics and training
numpy == 1.23.5
scipy == 1.10.1
tensorflow == 2.11.1
tqdm == 4.64.1

# NLP / tokenizer stack used by QuestionBatcher and EmbeddingServer
transformers == 4.35.2
tokenizers < 0.15
safetensors == 0.3.3
huggingface-hub == 0.17.3

# Data loading, splitting, and cache format (.parquet)
pandas == 2.3.1
scikit-learn == 1.6.1
fastparquet == 2024.11.0

# Config and experiment tracking
omegaconf == 2.3.0
wandb >= 0.15.0

# Plotting and visualization (optional, for analysis)
matplotlib >= 3.9.4